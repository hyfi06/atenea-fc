# Fixes de sesión, agendado, horario y estilo — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar los 6 fixes acotados aprobados en el spec: logout que termina en `/login`, botón de regreso de Login, ventana de 2h no aplicada al listar disponibilidad, estilo del botón "+ Agregar" en Mis materias, domingo visible en Mi horario, y `<select>` casi ilegible en Chrome/Opera/Edge + Linux Mint.

**Architecture:** Seis cambios independientes, sin dependencias entre sí — cada tarea se puede ejecutar, probar y comitear por separado, en cualquier orden. Cinco tocan sólo `frontend/`, una sólo `backend/`. Ninguna toca la misma línea de código que otra: no hay riesgo de conflicto de merge entre tareas.

**Tech Stack:** Backend: Django + Django REST Framework, tests con `django.test`/`rest_framework.test.APITestCase` vía `manage.py test`. Frontend: React + TypeScript + React Router v6, tests con Vitest + Testing Library (`fireEvent`, sin `@testing-library/user-event`) vía `vitest run`. Estilos con Tailwind v4 (`@theme`) sobre tokens Material Design 3 en `frontend/src/index.css`.

**Spec:** `docs/superpowers/specs/2026-08-24-fixes-design.md`

## Global Constraints

- Cada commit sigue el formato `[tipo][scope] resumen` + bullets + `Signed-off-by` (generado con `git commit -s`, NO se escribe el trailer a mano). Tipos usados en este plan: `fix`, `feat`, `test`. Scope: `backend` o `frontend` según la tarea.
- Ninguna tarea agrega ADR ni deuda técnica nueva — todas son correcciones dentro de decisiones ya vigentes (spec, sección Contexto).
- La tarea 3 (backend) reutiliza `VENTANA_MINIMA_ANTICIPACION`, importada de `asesorias.models` — no se redefine el valor de 2 horas en ningún otro lugar.
- No se agregan dependencias nuevas a `frontend/package.json` ni a `backend/pyproject.toml` en ninguna tarea.
- Backend: comandos de test se corren desde `backend/` con el intérprete del venv del proyecto: `.venv/bin/python manage.py test <ruta.punteada> -v 2`. `DJANGO_SETTINGS_MODULE` ya resuelve a `config.settings.dev` por default en `manage.py`, no hace falta exportarlo.
- Frontend: comandos de test se corren desde `frontend/`: `npx vitest run <ruta/al/archivo>`. Verificación final de cada tarea frontend además corre `npm run build` (`tsc -b && vite build`) y `npm run lint` (`oxlint`) desde `frontend/`.
- Todas las rutas de archivo en este plan son relativas a la raíz del repo salvo que se indique lo contrario.

---

## File Structure

- Modify `backend/asesorias/views.py` — filtro de ventana de 2h en `BuscarDisponibilidadView.get()` (Tarea 3).
- Modify `backend/asesorias/tests/test_api_busqueda.py` — 2 casos nuevos para el filtro de 2h (Tarea 3).
- Modify `frontend/src/auth/RutaProtegida.tsx` — destino de redirect sin sesión: `/` en vez de `/login`, en las 5 guardas exportadas (Tarea 1).
- Modify `frontend/src/auth/RutaProtegida.test.tsx` — asserts actualizados a `/` (Tarea 1).
- Modify `frontend/src/screens/Login.tsx` — botón de regreso: `← Inicio` fijo a `/` en vez del ícono `navigate(-1)` (Tarea 2).
- Modify `frontend/src/screens/Login.test.tsx` — ruta `/` agregada al harness + caso nuevo del botón (Tarea 2).
- Modify `frontend/src/features/asesorias/screens/MisMaterias.tsx` — clase del botón `+ Agregar` (Tarea 4).
- Modify `frontend/src/features/asesorias/screens/MisMaterias.test.tsx` — caso nuevo de estilo del botón (Tarea 4).
- Modify `frontend/src/features/asesorias/screens/MiHorario.tsx` — arreglos `DIAS`/`DIAS_CORTOS` sin domingo (Tarea 5).
- Modify `frontend/src/features/asesorias/screens/MiHorario.test.tsx` — 2 asserts de conteo/ausencia de pestaña (Tarea 5).
- Modify `frontend/src/index.css` — `color-scheme: dark` + reglas `select`/`option` (Tarea 6, sin test automatizado — ver spec).

---

### Task 1: Guardas de ruta redirigen a `/` en vez de `/login`

**Files:**
- Modify: `frontend/src/auth/RutaProtegida.tsx`
- Test: `frontend/src/auth/RutaProtegida.test.tsx`

**Interfaces:**
- Consumes: ninguno (usa `useAuth()` y `<Navigate>`, ya existentes).
- Produces: ninguno consumido por otras tareas de este plan (independiente).

- [ ] **Step 1: Actualizar el archivo de test completo con el nuevo destino esperado**

Reemplazar **todo el contenido** de `frontend/src/auth/RutaProtegida.test.tsx` por:

```tsx
import { describe, it, expect, vi, afterEach } from 'vitest'
import { render, screen } from '@testing-library/react'
import { MemoryRouter, Route, Routes } from 'react-router-dom'
import { AuthProvider } from './AuthContext'
import { RutaDeAsesor, RutaDeAsesorias, RutaDeSAE, RutaConSesion } from './RutaProtegida'
import * as client from '../api/client'
import { usuarioDePrueba, usuarioSAE } from '../test/factories'
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'

const queryClient = new QueryClient({ defaultOptions: { queries: { retry: false } } })

function Proveedores({ children }: { children: React.ReactNode }) {
  return (
    <QueryClientProvider client={queryClient}>
      <AuthProvider>{children}</AuthProvider>
    </QueryClientProvider>
  )
}

function montar() {
  render(
    <Proveedores>
      <MemoryRouter initialEntries={['/asesorias']}>
        <Routes>
          <Route
            path="/asesorias"
            element={
              <RutaDeAsesor>
                <p>panel del asesor</p>
              </RutaDeAsesor>
            }
          />
          <Route path="/home" element={<p>pantalla home</p>} />
          <Route path="/" element={<p>pantalla landing</p>} />
        </Routes>
      </MemoryRouter>
    </Proveedores>,
  )
}

describe('RutaDeAsesor', () => {
  afterEach(() => vi.restoreAllMocks())

  it('deja pasar al asesor académico', async () => {
    vi.spyOn(client, 'apiGet').mockResolvedValue(
      usuarioDePrueba({ roles: ['academico', 'asesor_academico'] }),
    )

    montar()

    expect(await screen.findByText('panel del asesor')).toBeInTheDocument()
  })

  it('manda a Home a quien tiene sesión pero no es asesor', async () => {
    vi.spyOn(client, 'apiGet').mockResolvedValue(usuarioDePrueba({ roles: ['alumno'] }))

    montar()

    expect(await screen.findByText('pantalla home')).toBeInTheDocument()
    expect(screen.queryByText('panel del asesor')).not.toBeInTheDocument()
  })

  it('manda a la landing a quien no tiene sesión', async () => {
    vi.spyOn(client, 'apiGet').mockRejectedValue(new client.ApiError(401, { detail: 'no autenticado' }))

    montar()

    expect(await screen.findByText('pantalla landing')).toBeInTheDocument()
  })

  it('deja pasar al asesor aunque su perfil esté inactivo', async () => {
    vi.spyOn(client, 'apiGet').mockResolvedValue(
      usuarioDePrueba({
        roles: ['academico', 'asesor_academico'],
        perfil_asesor_academico: { id: 3, area: 2, area_nombre: 'Matemáticas', activo: false },
      }),
    )

    montar()

    expect(await screen.findByText('panel del asesor')).toBeInTheDocument()
  })
})

function montarAsesorias() {
  render(
    <Proveedores>
      <MemoryRouter initialEntries={['/asesorias']}>
        <Routes>
          <Route
            path="/asesorias"
            element={
              <RutaDeAsesorias>
                <p>vista de asesorías</p>
              </RutaDeAsesorias>
            }
          />
          <Route path="/home" element={<p>pantalla home</p>} />
          <Route path="/" element={<p>pantalla landing</p>} />
        </Routes>
      </MemoryRouter>
    </Proveedores>,
  )
}

describe('RutaDeAsesorias', () => {
  afterEach(() => vi.restoreAllMocks())

  it('deja pasar al alumno', async () => {
    vi.spyOn(client, 'apiGet').mockResolvedValue(usuarioDePrueba({ roles: ['alumno'] }))
    montarAsesorias()
    expect(await screen.findByText('vista de asesorías')).toBeInTheDocument()
  })

  it('deja pasar al asesor', async () => {
    vi.spyOn(client, 'apiGet').mockResolvedValue(
      usuarioDePrueba({ roles: ['academico', 'asesor_academico'] }),
    )
    montarAsesorias()
    expect(await screen.findByText('vista de asesorías')).toBeInTheDocument()
  })

  it('manda a Home a quien no es alumno ni asesor ni académico', async () => {
    vi.spyOn(client, 'apiGet').mockResolvedValue(usuarioDePrueba({ roles: [] }))
    montarAsesorias()
    expect(await screen.findByText('pantalla home')).toBeInTheDocument()
    expect(screen.queryByText('vista de asesorías')).not.toBeInTheDocument()
  })

  it('manda a la landing a quien no tiene sesión', async () => {
    vi.spyOn(client, 'apiGet').mockRejectedValue(new client.ApiError(401, { detail: 'no autenticado' }))
    montarAsesorias()
    expect(await screen.findByText('pantalla landing')).toBeInTheDocument()
  })

  it('RutaDeAsesorias deja pasar al académico sin perfil de asesor', async () => {
    // Es la puerta al autoservicio de registro: sin esto, un académico que
    // toca el tile de Asesorías rebota a /home y no encuentra dónde darse de alta.
    vi.spyOn(client, 'apiGet').mockResolvedValue(usuarioDePrueba({ roles: ['academico'] }))
    montarAsesorias()
    expect(await screen.findByText('vista de asesorías')).toBeInTheDocument()
  })
})

function montarSAE() {
  render(
    <Proveedores>
      <MemoryRouter initialEntries={['/sae/asesorias']}>
        <Routes>
          <Route
            path="/sae/asesorias"
            element={
              <RutaDeSAE>
                <p>área SAE</p>
              </RutaDeSAE>
            }
          />
          <Route path="/home" element={<p>pantalla home</p>} />
          <Route path="/" element={<p>pantalla landing</p>} />
        </Routes>
      </MemoryRouter>
    </Proveedores>,
  )
}

describe('RutaDeSAE', () => {
  afterEach(() => vi.restoreAllMocks())

  it('deja pasar al miembro de la SAE', async () => {
    vi.spyOn(client, 'apiGet').mockResolvedValue(usuarioSAE())
    montarSAE()
    expect(await screen.findByText('área SAE')).toBeInTheDocument()
  })

  it('manda a Home a quien tiene sesión pero no es SAE', async () => {
    vi.spyOn(client, 'apiGet').mockResolvedValue(
      usuarioDePrueba({ roles: ['alumno', 'asesor_academico'] }),
    )
    montarSAE()
    expect(await screen.findByText('pantalla home')).toBeInTheDocument()
    expect(screen.queryByText('área SAE')).not.toBeInTheDocument()
  })

  it('manda a la landing a quien no tiene sesión', async () => {
    vi.spyOn(client, 'apiGet').mockRejectedValue(new client.ApiError(401, { detail: 'no autenticado' }))
    montarSAE()
    expect(await screen.findByText('pantalla landing')).toBeInTheDocument()
  })
})

function montarConSesion() {
  render(
    <Proveedores>
      <MemoryRouter initialEntries={['/home']}>
        <Routes>
          <Route
            path="/home"
            element={
              <RutaConSesion>
                <p>pantalla home</p>
              </RutaConSesion>
            }
          />
          <Route path="/" element={<p>pantalla landing</p>} />
        </Routes>
      </MemoryRouter>
    </Proveedores>,
  )
}

describe('RutaConSesion', () => {
  afterEach(() => vi.restoreAllMocks())

  it('deja pasar a cualquier usuario con sesión', async () => {
    vi.spyOn(client, 'apiGet').mockResolvedValue(usuarioDePrueba({ roles: ['alumno'] }))
    montarConSesion()
    expect(await screen.findByText('pantalla home')).toBeInTheDocument()
  })

  it('deja pasar a un usuario con sesión aunque no tenga roles', async () => {
    vi.spyOn(client, 'apiGet').mockResolvedValue(usuarioDePrueba({ roles: [] }))
    montarConSesion()
    expect(await screen.findByText('pantalla home')).toBeInTheDocument()
    expect(screen.queryByText('pantalla landing')).not.toBeInTheDocument()
  })

  it('manda a la landing a quien no tiene sesión', async () => {
    vi.spyOn(client, 'apiGet').mockRejectedValue(new client.ApiError(401, { detail: 'no autenticado' }))
    montarConSesion()
    expect(await screen.findByText('pantalla landing')).toBeInTheDocument()
    expect(screen.queryByText('pantalla home')).not.toBeInTheDocument()
  })

  it('muestra el spinner mientras se resuelve la sesión', () => {
    vi.spyOn(client, 'apiGet').mockReturnValue(new Promise(() => {}))
    montarConSesion()
    expect(screen.getByLabelText('Cargando')).toBeInTheDocument()
    expect(screen.queryByText('pantalla home')).not.toBeInTheDocument()
  })
})
```

- [ ] **Step 2: Correr los tests y confirmar que fallan**

Run (desde `frontend/`): `npx vitest run src/auth/RutaProtegida.test.tsx`
Expected: FAIL en los 4 tests `'manda a la landing a quien no tiene sesión'` (uno por cada `describe`) — no encuentran el texto `'pantalla landing'`, porque el código actual todavía navega a `/login`, ruta que ya no existe en el harness de test. Los demás 12 tests siguen en verde.

- [ ] **Step 3: Reemplazar el contenido de `RutaProtegida.tsx`**

Reemplazar **todo el contenido** de `frontend/src/auth/RutaProtegida.tsx` por:

```tsx
import type { ReactNode } from 'react'
import { Navigate } from 'react-router-dom'
import { useAuth } from './AuthContext'
import { useEsAsesor, useEsAlumno, useEsMiembroSAE, useEsAcademico } from './rol'
import { PantallaCargando } from '../components/PantallaCargando'

export function RutaConSesion({ children }: { children: ReactNode }) {
  const { status } = useAuth()

  // Solo exige sesión, sin rol. `/home` es la página de aterrizaje de cualquier
  // usuario autenticado (los guards por rol redirigen aquí al fallar el rol), así
  // que no puede usar esos guards sin provocar un loop de redirección.
  if (status === 'loading') return <PantallaCargando />
  if (status === 'unauthenticated') {
    return <Navigate to="/" replace />
  }

  return <>{children}</>
}

export function RutaDeAsesor({ children }: { children: ReactNode }) {
  const { status } = useAuth()
  const esAsesor = useEsAsesor()

  // Antes había dos estados de carga (el de la sesión y el del sondeo de rol).
  // Ahora el rol llega con la sesión, así que `status` es la única compuerta.
  if (status === 'loading') return <PantallaCargando />
  if (status === 'unauthenticated') {
    return <Navigate to="/" replace />
  }
  if (!esAsesor) return <Navigate to="/home" replace />

  return <>{children}</>
}

export function RutaDeAsesorias({ children }: { children: ReactNode }) {
  const { status } = useAuth()
  const esAsesor = useEsAsesor()
  const esAlumno = useEsAlumno()
  const esAcademico = useEsAcademico()

  if (status === 'loading') return <PantallaCargando />
  if (status === 'unauthenticated') {
    return <Navigate to="/" replace />
  }
  if (!esAsesor && !esAlumno && !esAcademico) return <Navigate to="/home" replace />

  return <>{children}</>
}


export function RutaDeSAE({ children }: { children: ReactNode }) {
  const { status } = useAuth()
  const esMiembroSAE = useEsMiembroSAE()

  if (status === 'loading') return <PantallaCargando />
  if (status === 'unauthenticated') {
    return <Navigate to="/" replace />
  }
  if (!esMiembroSAE) return <Navigate to="/home" replace />

  return <>{children}</>
}

export function RutaDeAcademico({ children }: { children: ReactNode }) {
  const { status } = useAuth()
  const esAcademico = useEsAcademico()

  if (status === 'loading') return <PantallaCargando />
  if (status === 'unauthenticated') {
    return <Navigate to="/" replace />
  }
  if (!esAcademico) return <Navigate to="/home" replace />

  return <>{children}</>
}
```

- [ ] **Step 4: Correr los tests y confirmar que pasan**

Run: `npx vitest run src/auth/RutaProtegida.test.tsx`
Expected: PASS — 16 tests en verde (4 en `RutaDeAsesor`, 5 en `RutaDeAsesorias`, 3 en `RutaDeSAE`, 4 en `RutaConSesion`).

También correr, desde `frontend/`: `npx vitest run src/components/MenuUsuario.test.tsx` — debe seguir en verde sin cambios (ese archivo ya esperaba `pantalla landing`, no `pantalla login`).

- [ ] **Step 5: Verificación final y commit**

Run: `npm run build` y `npm run lint` (ambos desde `frontend/`) — deben terminar sin errores nuevos.

```bash
git add frontend/src/auth/RutaProtegida.tsx frontend/src/auth/RutaProtegida.test.tsx
git commit -s -m "[fix][frontend] redirigir a la landing en vez de /login cuando no hay sesión

- las 5 guardas de RutaProtegida.tsx (RutaConSesion, RutaDeAsesor, RutaDeAsesorias, RutaDeSAE, RutaDeAcademico) redirigen a / en vez de /login
- se quita el state {from: location} que pasaban a /login: nunca se leía en ningún consumidor
- actualiza los 16 tests de RutaProtegida.test.tsx al nuevo destino"
```

---

### Task 2: Botón de regreso de Login usa el patrón "← Inicio"

**Files:**
- Modify: `frontend/src/screens/Login.tsx`
- Test: `frontend/src/screens/Login.test.tsx`

**Interfaces:**
- Consumes: ninguno.
- Produces: ninguno consumido por otras tareas de este plan (independiente).

- [ ] **Step 1: Agregar la ruta `/` al harness de test y el caso nuevo**

En `frontend/src/screens/Login.test.tsx`, reemplazar este bloque exacto (dentro de `montar`, líneas 30-38 del archivo actual):

```tsx
  render(
    <MemoryRouter initialEntries={['/login']}>
      <Routes>
        <Route path="/login" element={<Login />} />
        <Route path="/home" element={<p>pantalla home</p>} />
        <Route path="/forgot-password" element={<p>pantalla recuperar</p>} />
      </Routes>
    </MemoryRouter>,
  )
```

por:

```tsx
  render(
    <MemoryRouter initialEntries={['/login']}>
      <Routes>
        <Route path="/login" element={<Login />} />
        <Route path="/home" element={<p>pantalla home</p>} />
        <Route path="/forgot-password" element={<p>pantalla recuperar</p>} />
        <Route path="/" element={<p>pantalla landing</p>} />
      </Routes>
    </MemoryRouter>,
  )
```

Luego, agregar este caso nuevo como último `it(...)` dentro del `describe('Login', ...)`, justo antes del `})` final que cierra el `describe` (después del caso `'el botón de contraseña olvidada navega a /forgot-password'`):

```tsx
  it('el botón de regreso navega a la landing', async () => {
    montar()

    fireEvent.click(screen.getByRole('button', { name: '← Inicio' }))

    expect(await screen.findByText('pantalla landing')).toBeInTheDocument()
  })
```

- [ ] **Step 2: Correr los tests y confirmar que el caso nuevo falla**

Run (desde `frontend/`): `npx vitest run src/screens/Login.test.tsx`
Expected: FAIL sólo en `'el botón de regreso navega a la landing'` — `getByRole('button', { name: '← Inicio' })` no encuentra ningún botón con ese nombre accesible (el botón actual sólo tiene un ícono SVG y `aria-label="Volver"`). Los demás 8 tests siguen en verde.

- [ ] **Step 3: Reemplazar el botón de regreso en `Login.tsx`**

En `frontend/src/screens/Login.tsx`, reemplazar este bloque exacto (líneas 53-65 del archivo actual):

```tsx
  return (
    <main className="flex min-h-svh flex-col px-6 py-6">
      <button
        type="button"
        onClick={() => navigate(-1)}
        aria-label="Volver"
        className={`mb-8 flex h-9 w-9 items-center justify-center rounded-full text-on-background ${FOCO_VISIBLE}`}
      >
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth={2} strokeLinecap="round" strokeLinejoin="round" className="h-5 w-5" aria-hidden>
          <path d="M15 19 L8 12 L15 5" />
        </svg>
      </button>

      <form onSubmit={handleSubmit} className="flex flex-col gap-6">
```

por:

```tsx
  return (
    <main className="flex min-h-svh flex-col px-6 py-6">
      <button
        type="button"
        onClick={() => navigate('/')}
        className="foco-visible mb-8 w-fit min-h-11 text-sm text-primary"
      >
        ← Inicio
      </button>

      <form onSubmit={handleSubmit} className="flex flex-col gap-6">
```

No se toca el `import { CampoTexto, FOCO_VISIBLE } from '../components/ui/CampoTexto'` de la línea 6: `FOCO_VISIBLE` lo sigue usando el botón "¿Olvidaste tu contraseña?" más abajo en el mismo archivo.

- [ ] **Step 4: Correr los tests y confirmar que pasan**

Run: `npx vitest run src/screens/Login.test.tsx`
Expected: PASS — 9 tests en verde.

- [ ] **Step 5: Verificación final y commit**

Run: `npm run build` y `npm run lint` (desde `frontend/`) — sin errores nuevos.

```bash
git add frontend/src/screens/Login.tsx frontend/src/screens/Login.test.tsx
git commit -s -m "[fix][frontend] reemplazar el botón de regreso de Login por ← Inicio fijo a la landing

- quita el botón circular con ícono que usaba navigate(-1) (dependía del historial del navegador)
- lo reemplaza por el mismo patrón textual ← Inicio que ya usan las pantallas de Asesorías, con destino fijo a /
- agrega el caso de test del botón nuevo y la ruta / al harness de Login.test.tsx"
```

---

### Task 3: No devolver en la búsqueda los bloques a menos de 2h de anticipación

**Files:**
- Modify: `backend/asesorias/views.py`
- Test: `backend/asesorias/tests/test_api_busqueda.py`

**Interfaces:**
- Consumes: `VENTANA_MINIMA_ANTICIPACION` (constante `datetime.timedelta`, ya definida en `backend/asesorias/models.py:23`, exportada desde ese módulo).
- Produces: ninguno consumido por otras tareas de este plan (independiente).

- [ ] **Step 1: Agregar los 2 casos de test nuevos**

En `backend/asesorias/tests/test_api_busqueda.py`, dentro de la clase `BuscarDisponibilidadApiTests`, insertar los siguientes dos métodos nuevos **justo después** del método `test_filtra_por_asesor` (y antes de `test_miembro_sae_puede_usar_la_busqueda`). El método `test_filtra_por_asesor` actual termina así (para ubicar el punto de inserción):

```python
        self.client.force_authenticate(user=self.alumno_user)
        response = self.client.get(
            f"/api/asesorias/disponibilidad/buscar/?materia={self.materia.id}&asesor={self.registro.id}"
        )
        registros = {r["registro_id"] for r in response.data}
        self.assertEqual(registros, {self.registro.id})
```

Insertar inmediatamente después de ese bloque (antes del `def test_miembro_sae_puede_usar_la_busqueda`):

```python

    def test_no_devuelve_bloque_de_hoy_a_menos_de_dos_horas(self):
        otro_user = User.objects.create_user(email="asesor-pronto@ciencias.unam.mx", password="x")
        PerfilAcademico.objects.create(user=otro_user, numero_trabajador="88888")
        otro_asesor = PerfilAsesorAcademico.objects.create(user=otro_user, area=self.area)
        otro_registro = RegistroAsesor.objects.create(asesor=otro_asesor, semestre=semestre_vigente())
        otro_registro.agregar_materia(self.materia)

        # Redondea a la hora en punto (rejilla de Disponibilidad): la
        # separación real contra "ahora" queda entre 30 y 90 min, siempre
        # dentro de la ventana mínima de 2h. Mismo criterio que
        # `_bloque_que_arranca_en_menos_de_dos_horas` en test_disponibilidad.py.
        pronto = timezone.localtime() + datetime.timedelta(minutes=90)
        Disponibilidad.objects.create(
            registro=otro_registro, dia_semana=pronto.weekday(), hora_inicio=datetime.time(pronto.hour, 0),
            formato="virtual", liga_virtual="https://meet.example.com/pronto",
        )

        self.client.force_authenticate(user=self.alumno_user)
        response = self.client.get(
            f"/api/asesorias/disponibilidad/buscar/?materia={self.materia.id}"
        )

        resultados_de_ese_dia = [r for r in response.data if r["fecha"] == str(pronto.date())]
        self.assertEqual(resultados_de_ese_dia, [])

    def test_si_devuelve_bloque_de_hoy_a_mas_de_dos_horas(self):
        otro_user = User.objects.create_user(email="asesor-lejano@ciencias.unam.mx", password="x")
        PerfilAcademico.objects.create(user=otro_user, numero_trabajador="77777")
        otro_asesor = PerfilAsesorAcademico.objects.create(user=otro_user, area=self.area)
        otro_registro = RegistroAsesor.objects.create(asesor=otro_asesor, semestre=semestre_vigente())
        otro_registro.agregar_materia(self.materia)

        # Redondeado a la hora en punto, la separación real contra "ahora"
        # queda entre 2.5 y 3.5h, siempre fuera de la ventana mínima de 2h.
        lejano = timezone.localtime() + datetime.timedelta(hours=3)
        Disponibilidad.objects.create(
            registro=otro_registro, dia_semana=lejano.weekday(), hora_inicio=datetime.time(lejano.hour, 0),
            formato="virtual", liga_virtual="https://meet.example.com/lejano",
        )

        self.client.force_authenticate(user=self.alumno_user)
        response = self.client.get(
            f"/api/asesorias/disponibilidad/buscar/?materia={self.materia.id}"
        )

        resultados_de_ese_dia = [r for r in response.data if r["fecha"] == str(lejano.date())]
        self.assertEqual(len(resultados_de_ese_dia), 1)
```

No hace falta agregar ningún `import` nuevo: `datetime`, `User`, `PerfilAcademico`, `PerfilAsesorAcademico`, `RegistroAsesor`, `Disponibilidad`, `semestre_vigente` y `timezone` ya están importados al inicio del archivo.

- [ ] **Step 2: Correr los tests y confirmar el estado esperado**

Run (desde `backend/`): `.venv/bin/python manage.py test asesorias.tests.test_api_busqueda.BuscarDisponibilidadApiTests -v 2`
Expected: `test_no_devuelve_bloque_de_hoy_a_menos_de_dos_horas` **FALLA** (`resultados_de_ese_dia` no está vacío: el bloque de "pronto" todavía aparece porque el filtro no existe). `test_si_devuelve_bloque_de_hoy_a_mas_de_dos_horas` **ya pasa** hoy (nada lo excluye todavía) — es esperado, ese caso confirma que el fix no debe volverse demasiado agresivo. Los otros 9 tests ya existentes de la clase siguen en verde.

- [ ] **Step 3: Implementar el filtro en `BuscarDisponibilidadView`**

En `backend/asesorias/views.py`, reemplazar la línea de import de `.models` (línea 18 del archivo actual):

```python
from .models import Asesoria, Disponibilidad, PerfilAsesorAcademico, RegistroAsesor
```

por:

```python
from .models import Asesoria, Disponibilidad, PerfilAsesorAcademico, RegistroAsesor, VENTANA_MINIMA_ANTICIPACION
```

Luego, dentro de `BuscarDisponibilidadView.get()`, reemplazar este bloque exacto:

```python
        inicio, fin = ventana_agendable()
        ocupados = set(
            Asesoria.objects.filter(fecha__range=(inicio, fin))
            .exclude(estado="cancelada")
            .values_list("disponibilidad_id", "fecha")
        )

        resultados = []
        fecha_cursor = inicio
        while fecha_cursor <= fin:
            dia_semana = fecha_cursor.weekday()
            for disp in disponibilidades:
                if disp.dia_semana != dia_semana:
                    continue
                if (disp.id, fecha_cursor) in ocupados:
                    continue
                resultados.append({
                    "registro_id": disp.registro_id,
                    "asesor_nombre": disp.registro.asesor.user.nombre_completo,
                    "disponibilidad_id": disp.id,
                    "fecha": fecha_cursor,
                    "hora_inicio": disp.hora_inicio,
                    "hora_fin": disp.hora_fin,
                    "formato": disp.formato,
                    "ubicacion": disp.ubicacion,
                    "liga_virtual": disp.liga_virtual,
                })
            fecha_cursor += datetime.timedelta(days=1)

        return Response(ResultadoBusquedaSerializer(resultados, many=True).data)
```

por:

```python
        inicio, fin = ventana_agendable()
        ahora = timezone.now()
        ocupados = set(
            Asesoria.objects.filter(fecha__range=(inicio, fin))
            .exclude(estado="cancelada")
            .values_list("disponibilidad_id", "fecha")
        )

        resultados = []
        fecha_cursor = inicio
        while fecha_cursor <= fin:
            dia_semana = fecha_cursor.weekday()
            for disp in disponibilidades:
                if disp.dia_semana != dia_semana:
                    continue
                if (disp.id, fecha_cursor) in ocupados:
                    continue
                # Mismo predicado que `Asesoria.clean()` (deuda 0003): lo que
                # se ofrece como agendable debe ser siempre agendable.
                momento_inicio = timezone.make_aware(
                    datetime.datetime.combine(fecha_cursor, disp.hora_inicio)
                )
                if ahora > momento_inicio - VENTANA_MINIMA_ANTICIPACION:
                    continue
                resultados.append({
                    "registro_id": disp.registro_id,
                    "asesor_nombre": disp.registro.asesor.user.nombre_completo,
                    "disponibilidad_id": disp.id,
                    "fecha": fecha_cursor,
                    "hora_inicio": disp.hora_inicio,
                    "hora_fin": disp.hora_fin,
                    "formato": disp.formato,
                    "ubicacion": disp.ubicacion,
                    "liga_virtual": disp.liga_virtual,
                })
            fecha_cursor += datetime.timedelta(days=1)

        return Response(ResultadoBusquedaSerializer(resultados, many=True).data)
```

`timezone` (de `django.utils`) y `datetime` ya están importados al inicio de `views.py` (líneas 1 y 7 del archivo actual) — no se agrega ningún import nuevo salvo `VENTANA_MINIMA_ANTICIPACION`.

- [ ] **Step 4: Correr toda la clase de test y confirmar que pasa**

Run: `.venv/bin/python manage.py test asesorias.tests.test_api_busqueda -v 2`
Expected: PASS — todos los tests del archivo en verde, incluidos los 2 nuevos.

También correr, desde `backend/`: `.venv/bin/python manage.py test asesorias -v 2` — la suite completa de la app `asesorias` debe seguir en verde (el filtro nuevo no debe romper `test_disponibilidad.py`, `test_api_disponibilidad.py`, `test_api_oferta.py` ni `test_api_flujo_completo.py`).

- [ ] **Step 5: Commit**

```bash
git add backend/asesorias/views.py backend/asesorias/tests/test_api_busqueda.py
git commit -s -m "[feat][backend] excluir de la búsqueda de disponibilidad los bloques a menos de 2h

- BuscarDisponibilidadView.get() ahora aplica el mismo predicado de VENTANA_MINIMA_ANTICIPACION que ya usa Asesoria.clean() al agendar
- antes sólo se validaba al confirmar; el alumno podía ver un bloque que luego fallaba al agendarlo
- agrega 2 tests: un bloque de hoy a <2h no aparece, uno a >2h sí"
```

---

### Task 4: Botón "+ Agregar" en Mis materias usa la píldora con borde

**Files:**
- Modify: `frontend/src/features/asesorias/screens/MisMaterias.tsx`
- Test: `frontend/src/features/asesorias/screens/MisMaterias.test.tsx`

**Interfaces:**
- Consumes: ninguno.
- Produces: ninguno consumido por otras tareas de este plan (independiente).

- [ ] **Step 1: Agregar el caso de test nuevo**

En `frontend/src/features/asesorias/screens/MisMaterias.test.tsx`, agregar este caso nuevo dentro de `describe('MisMaterias', ...)`, justo después del caso `'al tocar la fila deja de truncar, para que el nombre completo sea accesible en móvil'` (antes del caso `'quitar pide confirmación...'`):

```tsx
  it('el botón de agregar usa el mismo estilo de píldora con borde que sus vecinos', () => {
    montar()

    const boton = screen.getByRole('button', { name: '+ Agregar' })
    expect(boton).toHaveClass('rounded-full', 'border', 'border-outline', 'text-primary')
  })
```

- [ ] **Step 2: Correr los tests y confirmar que el caso nuevo falla**

Run (desde `frontend/`): `npx vitest run src/features/asesorias/screens/MisMaterias.test.tsx`
Expected: FAIL sólo en `'el botón de agregar usa el mismo estilo de píldora con borde que sus vecinos'` — la clase actual del botón (`foco-visible min-h-11 rounded-full px-2 text-sm font-medium text-primary`) no incluye `border` ni `border-outline`. Los demás 8 tests siguen en verde.

- [ ] **Step 3: Actualizar la clase del botón**

En `frontend/src/features/asesorias/screens/MisMaterias.tsx`, reemplazar este bloque exacto:

```tsx
        <button
          type="button"
          onClick={() => {
            setErrorAgregar(null)
            setDialogoAgregarAbierto(true)
          }}
          className="foco-visible min-h-11 rounded-full px-2 text-sm font-medium text-primary"
        >
          + Agregar
        </button>
```

por:

```tsx
        <button
          type="button"
          onClick={() => {
            setErrorAgregar(null)
            setDialogoAgregarAbierto(true)
          }}
          className="foco-visible min-h-11 rounded-full border border-outline px-3 text-sm font-medium text-primary"
        >
          + Agregar
        </button>
```

- [ ] **Step 4: Correr los tests y confirmar que pasan**

Run: `npx vitest run src/features/asesorias/screens/MisMaterias.test.tsx`
Expected: PASS — 9 tests en verde.

- [ ] **Step 5: Verificación final y commit**

Run: `npm run build` y `npm run lint` (desde `frontend/`) — sin errores nuevos.

```bash
git add frontend/src/features/asesorias/screens/MisMaterias.tsx frontend/src/features/asesorias/screens/MisMaterias.test.tsx
git commit -s -m "[fix][frontend] alinear el estilo del botón + Agregar de Mis materias con sus vecinos

- agrega border border-outline y sube px-2 a px-3, igual que Mis materias/Mi horario en Asesorias.tsx
- antes era el único botón de acción de la pantalla sin borde
- agrega test que verifica las clases de la píldora"
```

---

### Task 5: Ocultar domingo en Mi horario

**Files:**
- Modify: `frontend/src/features/asesorias/screens/MiHorario.tsx`
- Test: `frontend/src/features/asesorias/screens/MiHorario.test.tsx`

**Interfaces:**
- Consumes: ninguno.
- Produces: ninguno consumido por otras tareas de este plan (independiente).

- [ ] **Step 1: Actualizar los 2 tests de conteo de pestañas**

En `frontend/src/features/asesorias/screens/MiHorario.test.tsx`, reemplazar este bloque exacto:

```tsx
  it('muestra una pestaña por día y la instrucción fija arriba', () => {
    montar()

    expect(screen.getAllByRole('tab')).toHaveLength(7)
    expect(
      screen.getByText(
        'Cada celda es un horario disponible: toca para activarlo o editarlo. Para cambiar de día, usa las pestañas. Los cambios se autoguardan.',
      ),
    ).toBeInTheDocument()
  })
```

por:

```tsx
  it('muestra una pestaña por día (sin domingo) y la instrucción fija arriba', () => {
    montar()

    expect(screen.getAllByRole('tab')).toHaveLength(6)
    expect(screen.queryByRole('tab', { name: 'Dom' })).not.toBeInTheDocument()
    expect(
      screen.getByText(
        'Cada celda es un horario disponible: toca para activarlo o editarlo. Para cambiar de día, usa las pestañas. Los cambios se autoguardan.',
      ),
    ).toBeInTheDocument()
  })
```

Y reemplazar este otro bloque exacto:

```tsx
  it('en solo lectura pinta los bloques recibidos y conserva los 7 días', () => {
    montarSoloLectura([BLOQUE_LUNES])

    expect(screen.getAllByRole('tab')).toHaveLength(7)
    expect(screen.getByText('Salón O-221')).toBeInTheDocument()
  })
```

por:

```tsx
  it('en solo lectura pinta los bloques recibidos y tampoco muestra domingo', () => {
    montarSoloLectura([BLOQUE_LUNES])

    expect(screen.getAllByRole('tab')).toHaveLength(6)
    expect(screen.queryByRole('tab', { name: 'Dom' })).not.toBeInTheDocument()
    expect(screen.getByText('Salón O-221')).toBeInTheDocument()
  })
```

- [ ] **Step 2: Correr los tests y confirmar que fallan**

Run (desde `frontend/`): `npx vitest run src/features/asesorias/screens/MiHorario.test.tsx`
Expected: FAIL en ambos tests modificados — hoy hay 7 pestañas y `'Dom'` sí está presente. Los demás 12 tests del archivo siguen en verde.

- [ ] **Step 3: Quitar domingo de los arreglos de días**

En `frontend/src/features/asesorias/screens/MiHorario.tsx`, reemplazar estas dos líneas exactas (líneas 27-28 del archivo actual):

```ts
const DIAS_CORTOS = ['Lun', 'Mar', 'Mié', 'Jue', 'Vie', 'Sáb', 'Dom']
const DIAS = ['Lunes', 'Martes', 'Miércoles', 'Jueves', 'Viernes', 'Sábado', 'Domingo']
```

por:

```ts
const DIAS_CORTOS = ['Lun', 'Mar', 'Mié', 'Jue', 'Vie', 'Sáb']
const DIAS = ['Lunes', 'Martes', 'Miércoles', 'Jueves', 'Viernes', 'Sábado']
```

No se toca nada más del archivo: el único lugar donde se recorre el arreglo completo es `DIAS_CORTOS.map(...)` al renderizar las pestañas y las secciones de cada día, y ambos quedan automáticamente acotados a 6 días.

- [ ] **Step 4: Correr los tests y confirmar que pasan**

Run: `npx vitest run src/features/asesorias/screens/MiHorario.test.tsx`
Expected: PASS — 14 tests en verde.

- [ ] **Step 5: Verificación final y commit**

Run: `npm run build` y `npm run lint` (desde `frontend/`) — sin errores nuevos.

```bash
git add frontend/src/features/asesorias/screens/MiHorario.tsx frontend/src/features/asesorias/screens/MiHorario.test.tsx
git commit -s -m "[chore][frontend] ocultar domingo en Mi horario

- DIAS_CORTOS/DIAS en MiHorario.tsx pasan de 7 a 6 entradas
- aplica igual en modo edición del asesor y en modo consulta de SAE (mismo componente)
- puramente de frontend: no se toca dia_semana ni la validación de Disponibilidad en el backend
- actualiza los 2 tests que asumían 7 pestañas"
```

---

### Task 6: `color-scheme` explícito para legibilidad de `<select>` en Chromium/Linux

**Files:**
- Modify: `frontend/src/index.css`

**Interfaces:**
- Consumes: ninguno.
- Produces: ninguno consumido por otras tareas de este plan (independiente).

**Nota:** esta tarea no tiene test automatizado — el propio spec (`docs/superpowers/specs/2026-08-24-fixes-design.md`, sección 5) lo deja explícito: no hay forma razonable de probar contraste visual de un popup nativo del sistema operativo desde un test automatizado. La verificación es manual (Step 3).

- [ ] **Step 1: Agregar `color-scheme` y las reglas de `select`/`option`**

En `frontend/src/index.css`, reemplazar este bloque exacto (líneas 71-75 del archivo actual):

```css
:root {
  --ease-out: cubic-bezier(0.23, 1, 0.32, 1);
  --ease-in-out: cubic-bezier(0.77, 0, 0.175, 1);
  --ease-drawer: cubic-bezier(0.32, 0.72, 0, 1);
}
```

por:

```css
:root {
  --ease-out: cubic-bezier(0.23, 1, 0.32, 1);
  --ease-in-out: cubic-bezier(0.77, 0, 0.175, 1);
  --ease-drawer: cubic-bezier(0.32, 0.72, 0, 1);
  /* La app sólo tiene tema oscuro (sin variante clara). Sin esto, Chromium en
     Linux puede pintar el popup nativo de <select> con texto de la paleta
     clara sobre fondo oscuro del tema GTK del SO (o viceversa), casi
     ilegible — bug reportado con Chrome/Opera/Edge + Linux Mint. Fix
     best-effort: Chromium no da control total del popup nativo. */
  color-scheme: dark;
}

select {
  color-scheme: dark;
  background-color: var(--color-surface-container);
  color: var(--color-on-surface);
}

select option {
  background-color: var(--color-surface-container);
  color: var(--color-on-surface);
}
```

No hace falta tocar ningún componente: `frontend/src/features/asesorias/components/DialogoAgregarMateria.tsx` (el `<select id="carrera-materia">` de la captura original) y `frontend/src/features/asesorias/screens/AgendarAsesoria.tsx` (el `<select id="carrera-agendar">`) son ambos `<select>` nativos sin clases que contradigan estas reglas globales.

- [ ] **Step 2: Verificar build y lint**

Run (desde `frontend/`): `npm run build`
Expected: termina sin errores (cambio puramente de CSS, no toca TypeScript).

Run: `npm run lint`
Expected: sin errores nuevos.

Run también la suite completa para confirmar cero regresiones visuales rotas por assertions de clase: `npx vitest run`
Expected: PASS — mismo número de tests en verde que antes de esta tarea (el CSS global no cambia ninguna clase que los tests verifiquen).

- [ ] **Step 3: Verificación manual (documentar en el mensaje de commit que se hizo)**

En Chrome (o Opera/Edge) sobre Linux Mint (o cualquier Linux con tema GTK oscuro/claro mixto): abrir la pantalla "Mis materias" → botón "+ Agregar" → desplegar el `<select>` "Carrera" y confirmar que las opciones son legibles (texto con contraste suficiente contra el fondo). Repetir en la pantalla de agendar asesoría, paso "Confirma tu asesoría", `<select>` "Carrera". Si el contraste sigue siendo insuficiente en alguna combinación de tema del SO, **no es un blocker**: el spec ya documenta que es best-effort y que Chromium en Linux no da control total del popup nativo — se reporta como seguimiento, no se bloquea el commit por eso.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/index.css
git commit -s -m "[fix][frontend] declarar color-scheme oscuro para legibilidad de <select> en Chromium/Linux

- agrega color-scheme: dark en :root y en select, más background-color/color explícitos en select y option
- best-effort: Chromium en Linux no da control total del popup nativo del <select>, documentado en el spec
- sin cambios de componente: ambos <select> existentes (DialogoAgregarMateria, AgendarAsesoria) son nativos sin clases que lo contradigan"
```
